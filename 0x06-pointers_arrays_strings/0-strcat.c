#include "main.h"
#include <stdio.h>
#include <ctype.h>

/**
 * *_strcat - check the code.
 *@dest: is
 *@src: is
 * Return: Always 0.
 */

char *_strcat(char *dest, char *src)
{
	strcat(dest, src);
	return dest;
}
