#include "main.h"
#include <stdio.h>
#include <ctype.h>
#include <string.h>

/**
 * *_strcat - check the code.
 *@dest: is
 *@src: is
 * Return: Always 0.
 */
char *string_toupper(char *)
{
	int i = 0;

	while (s[i] != '\0')
	{
		s[i] = toupper(s[i]);
		i++;
	}
	return (s);

}
