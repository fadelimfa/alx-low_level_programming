#include <stdlib.h>
#include <stdio.h>
#include <ctype.h>
#include "main.h"
/**
 * print_sign - Prints if alphabet or not
 * @n: is the variable
 * Return: 0 or 1
 */
int _abs(n)
{
    int abs(n);
    return (0);
}
#include <stdio.h>
#include <stdlib.h>

int _abs(n) {
    int abs_num = abs(n);
    printf("%d\n", abs_num);
    return 0;
}

