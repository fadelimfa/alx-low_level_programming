#include <stdlib.h>
#include <stdio.h>
#include <ctype.h>
#include "main.h"
/**
 * print_sign - Prints if alphabet or not
 * @n: is the variable
 * Return: 0 or 1
 */
int _abs(int ab)
{
	double a = abs(ab);
	printf("%a",a);
}

