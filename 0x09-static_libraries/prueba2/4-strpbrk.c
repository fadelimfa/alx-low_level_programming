#include "main.h"
#include <stdio.h>
#include <ctype.h>
#include <string.h>

/**
 *_strpbrk - check the code.
 *@s: is
 *@accept: is
 * Return: Always 0.
 */
char *_strpbrk(char *s, char *accept)
{
	return (strpbrk(s, accept));
}
