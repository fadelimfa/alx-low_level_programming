#include "main.h"
#include <stdio.h>
#include <ctype.h>
#include <string.h>

/**
 *_strstr - check the code.
 *@haystack: is
 *@needle: is
 * Return: Always 0.
 */

char *_strstr(char *haystack, char *needle)
{
	return (strstr(haystack, needle));
}
